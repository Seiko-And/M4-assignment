/* File name: 03-triangle.js
   Creat a Looping a Triangle
   Displays a triangle with # within a console window:*/

// Define the variables
let triangle = '';

// Creat a loop that displays a triangle within a console window
for (let i =1; i <= 7; i++ ){
    triangle += '#';
    console.log(triangle);
}